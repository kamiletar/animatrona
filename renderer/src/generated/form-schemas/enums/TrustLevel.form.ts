// AUTO-GENERATED by @lena/zenstack-form-plugin
// DO NOT EDIT MANUALLY

import { z } from 'zod/v4'

/**
 * Zod схема для enum TrustLevel с UI метаданными
 */
export const TrustLevelFormSchema = z.enum(['BLOCKED', 'UNTRUSTED', 'KNOWN', 'TRUSTED', 'VERIFIED'])
  .meta({
    ui: {
      options: [
      { value: 'BLOCKED', label: 'Заблокирован — никакие операции не разрешены' },
      { value: 'UNTRUSTED', label: 'Не доверенный — по умолчанию' },
      { value: 'KNOWN', label: 'Известный — проверен пользователем' },
      { value: 'TRUSTED', label: 'Доверенный — автоматический импорт' },
      { value: 'VERIFIED', label: 'Верифицированный — полное доверие' }
      ]
    }
  })

/**
 * Метки для значений enum TrustLevel
 */
export const TrustLevelLabels = {
  BLOCKED: 'Заблокирован — никакие операции не разрешены',
  UNTRUSTED: 'Не доверенный — по умолчанию',
  KNOWN: 'Известный — проверен пользователем',
  TRUSTED: 'Доверенный — автоматический импорт',
  VERIFIED: 'Верифицированный — полное доверие'
} as const

/**
 * Тип значения enum
 */
export type TrustLevelValue = z.infer<typeof TrustLevelFormSchema>
