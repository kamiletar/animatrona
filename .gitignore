# База данных (локальные данные)
prisma/data/

# TypeScript build info
tsconfig.tsbuildinfo

# Electron build (корневая папка)
/app/
dist/
out/

# FFmpeg binaries (скачиваются скриптом download-ffmpeg.ts)
resources/ffmpeg/

# Kubo (IPFS) binaries (скачиваются при сборке)
resources/kubo/

# Chromaprint fpcalc (скачивается скриптом download-fpcalc.ts)
resources/fpcalc/

build.log
